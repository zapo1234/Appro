{% extends 'toto.html.twig' %}

{% block title %}Toto{% endblock %}

{% block body %}
    <h1>Lister un  toto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ toto.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ toto.label }}</td>
            </tr>
            <tr>
                <th>Completed</th>
                <td>{{ toto.completed }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ toto.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ toto.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('toto_index') }}">back to list</a>

    <a href="{{ path('toto_edit', {'id': toto.id}) }}">edit</a>
   
    {{ include('toto/_delete_form.html.twig') }}
{% endblock %}
